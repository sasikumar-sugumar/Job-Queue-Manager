info:
  build:
    artifact: project.artifactId@
    name: project.name@
    description: project.description@
    version: project.version@

security:
  user:
    name: admin
    password: admin

quartz:
  threadCount: 15

server:
  port: 9098
---

quartz:
  jobstore:
    clustered: false
